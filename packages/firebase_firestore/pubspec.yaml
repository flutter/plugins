name: firebase_firestore

description: Firestore plugin for Flutter.
author: Flutter Team <flutter-dev@googlegroups.com>
homepage: https://github.com/flutter/plugins/tree/master/packages/firebase_firestore
version: 0.0.1

flutter:
  plugin:
    androidPackage: io.flutter.plugins.firebase.firestore
    pluginClass: FirestorePlugin

dependencies:
  flutter:
    sdk: flutter
  meta: "^1.0.5"

dev_dependencies:
  test: 0.12.21

environment:
    sdk: ">=1.8.0 <2.0.0-dev.infinity"
