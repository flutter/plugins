name: firebase_auth_example
description: Demonstrates how to use the firebase_auth plugin.

dependencies:
  flutter:
    sdk: flutter
  firebase_auth:
    path: ../
  google_sign_in:
    path: ../../google_sign_in
  firebase_messaging:
    path: /Users/Siva/prayuta flutter fork/packages/firebase_messaging


flutter:
  uses-material-design: true
